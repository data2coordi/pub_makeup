
import  {  
		MakeUpClass
		} from './MakeUpClass.js';


/**
 * メイクアップクラスのインスタンスを作成します。
 *
 * @param {Array} js_grp_array グループの配列
 * @param {Array} js_color_array 色の配列
 * @param {Array} js_main_array メインの配列
 * @param {Document} document ドキュメントオブジェクト
 */
let makeup = new MakeUpClass(js_grp_array, js_color_array, js_main_array, document);

